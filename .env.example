# Copy this file to `.env` (which is gitignored) and set your key.
# The Day-4 reasoning layer reads ANTHROPIC_API_KEY at runtime to call
# claude-opus-4-8. The deterministic pipeline + auditor run WITHOUT a key.
# Never commit the real key.

ANTHROPIC_API_KEY=your-anthropic-api-key-here
